"""
    gmat(src, target)
---
Given the `plink` file set `src`.{bed,bim,fam}, this function calculate a **G**
matrix in 3 columns, i.e.,

- `raw col element-value`

and store the elements in target
"""
function gmat(src, target)
end
