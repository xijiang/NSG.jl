# NSG.jl

A Julia realization for NSG sheep genotypes manipulations

# Installation and to start
To run these codes, run below commands in a Linux terminal:
```bash
git clone https://github.com/xijiang/NSG.jl NSG
cd NSG
julia
```
```julia
]
activate .
using NSG
# Latest `plink` and `beagle.jar` will be downloaded.
# Binaries will also be compiled if not ready
```

# Raw data management

# QC

# Imputation

# G matrix calculation
